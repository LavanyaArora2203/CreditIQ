# URL of the deployed FastAPI backend (no trailing slash).
# Local dev: http://localhost:9000
# Production (set this in Vercel → Project Settings → Environment Variables):
VITE_LOAN_API_URL=https://loan-api.onrender.com
